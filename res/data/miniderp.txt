// blah blah
// minibasic - something to compile into zderp
//

#include "asm_screen.txt"
var x = 12;
extern func putch(c);
extern var y;

var hex = "0123456789ABCDEF";

while(x > 2){
	putch(hex[x]);
	x = x - 1;
	x--;
}

if (x == y){
	putch("yay");
}elif(x < y){
	putch("nay");
}else{
	putch("wah");
}

func bonk(x){
	return 2*x;
}

// dynamic vars
// 	ops assume the type
//		+ always works on ints
//		[] always on arrays
//		but .+ and .[] call op_add and op_array by user
